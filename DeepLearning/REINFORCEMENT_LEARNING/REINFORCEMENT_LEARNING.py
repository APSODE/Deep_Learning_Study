import tensorflow as TF
import tensorflow.keras as TFKE

import math as MT
import matplotlib as MTPL

import gym as GYM


class REINFORCEMENT_LEARNING:
    def __init__(self, STATE, ACTION, REWARD, TRANSITION_PROBABILITY, DISCOUNT_FACTOR):
        self.STATE = STATE
        self.ACTION = ACTION
        self.REWARD = REWARD
        self.TS_PB = TRANSITION_PROBABILITY
        self.DC_FT = DISCOUNT_FACTOR

    def VALUE_FUNC():
        pass


